## 2026-09-23: L96 DA benchmark default inflation per case -- S0 1.5, S1 2.0 (ETKF/EnKF)

**Summary:** ETKF/EnKF inflation now defaults per case, `L96_DA_INFLATION = {"s0": 1.5, "s1": 2.0}` (`evaluation/run_l96.py`), in the DA benchmark entry points `evaluate_all_l96.py` (`--enkf-inflation`/`--etkf-inflation`) and `eval_da_random_layout_l96.py` (`--inflation`). A single number still applies to both cases, and `"s0=..,s1=.."` sets them separately.
**Files modified:** `evaluation/run_l96.py` — `L96_DA_INFLATION`, `parse_case_inflation`, `inflation_tag`, per-case pool config in `run_and_cache_baselines` (a dict `inflation` in `enkf_config`/`etkf_config` is resolved per case); `evaluate_all_l96.py`, `eval_da_random_layout_l96.py` — per-case default; `AGENTS.md` — convention line; `tests/test_da_random_layout_l96.py` — 2 tests.
**Rationale:** The random-observing-system sweep (arrays 54845/54954) found 1.5 best on S0 at n_obs 10 and 100 for both filters, and 2.0-2.5 best on S1, where model error needs the extra spread. The P1 value 2.0 is S1-tuned: on S0 it over-disperses (spread/RMSE 1.1-1.9) and makes EnKF diverge at high obs counts. Cache names are unchanged for scalar inflation (`_inf2.0`) and distinct for the per-case default (`_infs0-1.5_s1-2.0`), so no existing cache is silently reused. The existing sbatch scripts pass `2.0` explicitly and reproduce their published numbers; `eval_da_obs_count_l96.py` (merged #241 sweep) keeps its fixed 2.0.
**Verification:** `pytest tests/test_da_random_layout_l96.py` — 13 passed; 1-window end-to-end `run_and_cache_baselines` check: S0 ETKF pool built with inflation 1.5, S1 with 2.0; `ruff check` on touched files clean.
