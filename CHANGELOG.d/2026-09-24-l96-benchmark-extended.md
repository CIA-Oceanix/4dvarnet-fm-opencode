## 2026-09-24: L96 extended benchmark -- training budget, observing-system dependence, SDA, hybrid, DA CRPS

**Summary:** Follow-up to the benchmark-default report on identical inputs: `reports/l96/outputs/l96_benchmark_extended.md` (+ n_obs / k figures). Headlines: the DirectUNet-M -> SDA2-M hybrid (validation-tuned tau0 0.1, gw 2) is the best scheme on both test sets (RMSE and CRPS); the 400-epoch default is too short (1200 epochs: every family -9..-19%, family gaps close; ~85% of a 3000-window gain is training length); DA wins only at <= 10 obs per window (S0); the marginal-value "6.2x vs 1.9x" becomes 6.4-7.0x vs 1.1-1.7x once DA gets fast_weights.
**Files modified:** `evaluation/baselines.py` -- analysis-ensemble CRPS in batched ETKF/EnKF (`_analysis_crps_step`), `_expand_batch_params` already; `evaluation/run_l96.py` -- CRPS saved with trajectories, slow-only fast-ring fill; `eval_da_random_layout_l96.py` -- `--r-var`, CRPS in summaries; `evaluate_all_l96.py` -- `--cases`; `evaluation/estimate_metrics.py` + `eval_neural_l96.py` / `eval_sda_l96.py` / `eval_sda_directunet_hybrid_l96.py` -- `--members-file scores` (per-window rmse/crps/spread instead of GB members), `--sigma-prior`; `data/lorenz96.py` + `train.py` + `conf/schema.py` -- `data.train_cache` (settings-checked, atomic); `scripts/` -- validation-set builder, multi-draw canonical test sets, checker (hybrid results, DA layouts by content), hard-link archive of the benchmark runs; configs -- `A3_sda3fix_monaiM_l96` (SDA3 with its DA-bias conditioning actually active; the old config trained bit-identical to SDA2), 1200-epoch and 3000-window arms, `L96B_predictstatecfm_monaiM`; `batch/` -- all run scripts; `docs/results/l96_benchmark_default_paper_audit.md` -- claims audit + 2026-09-24 update; tests for every code change.
**Rationale:** The benchmark default needed its protocol checked (validation-tuned eval settings, DA CRPS on the right ensemble, SDA3 bug) and its conclusions stress-tested across observing systems and training budgets before any P1 paper numbers are updated.
**Verification:** every learned result checked against its test set (dataset + truth) and every DA run against its layouts before rendering; `pytest` on all new/changed test files passes; `ruff check` clean on touched files.
