## 2026-09-23: L96 benchmark under the benchmark default -- canonical random test set, results report

**Summary:** One canonical random-observing-system test set shared by every scheme, a checker that every result must pass, and the first full benchmark under the benchmark default (#242): DA, DirectUNet/PredictStateCFM/VanillaCFM-M (3 seeds), the P1 fixed-obs checkpoints and the SDA family, each on the regular 30-obs set and the random set, S0 and S1. Report: `reports/l96/outputs/l96_benchmark_default.md`.
**Files modified:** `scripts/make_l96_canonical_testset.py` — builds the test set from the DA layouts (#243), refusing unless layouts are identical across runs, regenerate bit for bit from their seeds, and all non-obs fields equal the P1 cache; writes a per-field SHA-256 manifest; `scripts/check_l96_testset_consistency.py` — dataset path + truth check for learned results, window order + layout hashes for DA; `reports/l96/generate_l96_benchmark_default_report.py` + outputs — recomputes every metric, refuses to render on a consistency failure; `config/experiment/L96B_predictstatecfm_monaiM.yaml`; `evaluate_all_l96.py` — `--cases`; `batch/run_l96_canonical_rlayout_eval.sbatch`, `batch/run_l96b_psc_seeds.sbatch`, `batch/run_l96b_seeds.sbatch`, `batch/run_l96_da_regular_s0_inf15.sbatch`.
**Rationale:** Comparisons are only meaningful if every scheme sees the same 200 windows, obs mask, noise, forcings and parameters; the DA baselines had already been scored on #243's layouts, so the learned schemes are scored on exactly those. The regular-grid S0 ETKF/EnKF were rerun at the new S0 inflation 1.5.
**Verification:** checker: 17 learned + 2 DA results on the random set and all regular-grid results consistent; builder is deterministic (same SHA-256 on rebuild); `ruff check` clean on new scripts.
